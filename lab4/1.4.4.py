
В начале создается пустой список, соответственно если нет элементов нет и индексов,
при первой итерации в цикле в переменную i попадет 0
и произойдет попытка получения значения списка по несуществующему индексу,
что приведет к ошибке IndexError
